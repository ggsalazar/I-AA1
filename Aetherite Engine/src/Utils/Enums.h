#pragma once

enum class Scenes {
	GAME,
	TITLE
};

enum class Menus {
	GO,
	LEVSEL,
	MAIN,
	OPTIONS,
	PAUSE
};

enum class UIElems {
	DEFAULT,
	APPLY,
	BACK,
	FULLSCREEN,
	LEVSEL,
	LEV1,
	LEV2,
	LEV3,
	MAINMENU,
	MUSICV,
	OPTIONS,
	QUIT,
	RESOLUTION,
	RESUME,
	SFXV
};